#!/usr/bin/env node

// Repository entry point. The setup skill asset is the canonical standalone
// companion so local development and public installs run the same server.
import '../skills/setup-copy-ai-id-codex/assets/codex-server.mjs';
